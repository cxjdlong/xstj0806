# 开发记录（通讯录 App）

## 2026-09-10 建项（v1.0）
- 由记账 App 的安卓离线壳基座派生：`/opt/projects/contacts`（记账工程 `/opt/projects/ledger` 已本地备份）。
- 前端 Vue3 + Vite + SheetJS(xlsx)，安卓壳复用 WebViewAssetLoader（真 origin → localStorage 持久、断网可用）。
- 三栏目：通讯录（查询/15条分页/修改/删除确认/点电话拨打）、添加（多编码多电话 + 姓名可空）、备份（完全导出/全部备份/完全导入/备份列表10条分页）。
- 添加页查重：编码或电话命中已存在 → 自动载入该联系人资料 + 标红 + 提交二次确认覆盖。
- 原生桥 `window.Contacts`：saveXls(写 Download/通讯录备份, 返回真实路径)、deleteFile、dial(ACTION_DIAL)。
- 固定自签 keystore.p12（alias `contacts` / 密码 `contacts123`）→ 云打包签名一致，可覆盖安装保留数据。
- 逻辑自测：`frontend/test_logic.mjs`（查重 + Excel 往返，15 项全过）。
