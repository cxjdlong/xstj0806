# 开发记录（通讯录 App）

## 2026-09-10 建项（v1.0）
- 由记账 App 的安卓离线壳基座派生：`/opt/projects/contacts`（记账工程 `/opt/projects/ledger` 已本地备份）。
- 前端 Vue3 + Vite + SheetJS(xlsx)，安卓壳复用 WebViewAssetLoader（真 origin → localStorage 持久、断网可用）。
- 三栏目：通讯录（查询/15条分页/修改/删除确认/点电话拨打）、添加（多编码多电话 + 姓名可空）、备份（完全导出/全部备份/完全导入/备份列表10条分页）。
- 添加页查重：编码或电话命中已存在 → 自动载入该联系人资料 + 标红 + 提交二次确认覆盖。
- 原生桥 `window.Contacts`：saveXls(写 Download/通讯录备份, 返回真实路径)、deleteFile、dial(ACTION_DIAL)。
- 固定自签 keystore.p12（alias `contacts` / 密码 `contacts123`）→ 云打包签名一致，可覆盖安装保留数据。
- 逻辑自测：`frontend/test_logic.mjs`（查重 + Excel 往返，15 项全过）。

## 2026-09-10 图标
- 用户提供 8000x8000 白底方图 → 生成 mipmap 48/72/96/144/192 传统图标 + 108..432 自适应图标前景(mipmap-anydpi-v26, 白底, 图形占安全区 72/108)。

## 2026-09-10 体验修正（用户反馈）
- 查重打断输入：原「边打字边自动载入」改为 —— 打字只显示黄色软提示+标红不打断；点提示按钮/离开输入框/提交时才载入并二次确认覆盖；修改模式可「改为新增」脱离。
- 查重最小长度：编码 ≥3 位、电话 ≥5 位才参与判定，避免打字中途误命中。
- 备份栏按钮重复：「完全导出」「全部备份」合并为一个「全部备份」（存 Excel 到手机+显示路径+记入备份列表），保留「完全导入」。

## 2026-09-10 查重时机再修正（用户二次反馈）
- 用户要求：判断时机唯一 = **光标移出当前输入框(blur) 之后**。移除打字期间的实时软提示；blur 即判定，命中就载入现有资料+标红+提交二次确认。保留最小长度防误判与「改为新增」。

## 2026-09-10 列表压缩（用户反馈）
- 通讯录每页 15 条 → **7 条**；单行高度压缩（padding 12→7/10px、姓名 16→14.5px、chip 12.5→11.5px），单条约 59px。

## 2026-09-10 载入现有资料时保留新输入（用户反馈）
- 命中重复载入现有资料时，**不再丢弃本次新输入的编码/电话**：保留在表单里并标「新增」(蓝框+标签)，用户可留着（提交时一起写入该联系人）或点「－」删除（确认提示写明不加入）。提交确认框列出原资料 / 新输入项 / 最终资料。

## 2026-09-10 电脑网页版
- 新增 `npm run build:web`（vite-plugin-singlefile）→ 单文件 `index.html`（JS/CSS 内联，≈508KB），双击即用，不套 nginx。
- 产物入库为 `web/通讯录.html`；电脑端措辞调整（保存目标显示"电脑浏览器下载目录"）。
- 实测 file:// 打开：localStorage 可用、添加/刷新持久化、Excel 导出下载、Excel 导入全部正常。

## 2026-09-10 查询支持姓名
- 通讯录查询从「编码/电话」扩展为 **编码 / 电话 / 姓名** 三者包含即命中（大小写不敏感）；提示文案同步更新。

## 2026-09-10 电脑网页版改为桌面界面
- 用户要求：网页版要像网页应用程序、不做手机适配 → 新增桌面入口 `web.html` + `src/desktop/*`（顶栏/左侧导航/表格化/每页15条）与 `desktop.css`。
- 表单逻辑抽到 `src/useContactForm.js`，手机端 EditView 与桌面端 DEdit 共用（行为一致）。
- `vite.config.web.js` 入口改为 web.html，产物 `dist-web/web.html`（单文件）→ 入库为 `web/通讯录.html`。

## 2026-09-10 web版分页
- 桌面网页版通讯录表格分页 15 条 → **8 条/页**（备份列表仍 10 条/页）。

## 2026-09-10 web版浏览器图标
- 网页标签图标内联进单文件：`web.html` 里 `<link rel="icon" href="data:image/png;base64,...">`（64×64 PNG，≈7.9KB base64），产物仍只有一个 html、零外部引用。

## 2026-09-10 存储层重构（大数据不卡）
- 存储主库：localStorage → **IndexedDB**（去掉 5MB 限制、不做全量 JSON 序列化、异步不阻塞 UI）；旧 localStorage 数据自动迁移。
- 落盘：改 500ms 防抖 + 批量导入 pause/resume；页面隐藏/卸载前强制落盘。
- 列表性能：联系人对象 markRaw（不建深层代理），加载 1万条水合 ~0.2s；修改后显式 touchSave。
- 导入：建索引 + 批量插入；1万行 Excel 导入 0.73s。
- 备份：上限 30 条，数据 >5000 时旧快照释放（只留记录+Excel 路径）；生成 Excel 时按钮显示「处理中」。
- 数据文件：紧凑 JSON（>1000 条去掉缩进），1万条 1.5MB / 写入 30ms。

## 2026-09-10 手机端万级压测 + 大操作不假死
- 4倍CPU降速(≈中端手机)实测 1万条：加载+水合 1.2s(水合 190ms)、模糊查询(命中1112条) 56ms、翻页 79ms、新增落库 144ms、导出1万条Excel 1.0~2.6s、导入1万条 ~1s。
- 生成/解析 Excel 是同步重活 → 按钮先 await nextTick + 40ms 让「处理中…」渲染出来再干活，避免看起来像卡死（手机版+桌面版都加）。

## 2026-09-10 数据落成真正的 SQLite .db（离线网页版）
- 目录结构：`通讯录.html` + `db/通讯录数据.db`（SQLite）+ `backup/xxx.xlsx`（备份）。
- 用 sql.js（WASM 内联进单文件 html，产物 1.47MB）在浏览器里读写真 SQLite：表 contacts/backups/meta。
- 验证：Python sqlite3 能直接打开生成的 .db；1万条解析 138ms、回写 155ms；旧 通讯录数据.json 自动提示迁移。

## 2026-09-10 排查「没创建文件」
- db 文件名改 ASCII：`db/contacts.db`（旧 `通讯录数据.db` 仍兼容读取）。
- 加版本标识：界面左下角显示「版本 v1.1 · 构建时间」，用于确认用户手上是不是最新 html。
- 加「写入自检」按钮（备份页→数据文件面板）：逐步报告 浏览器支持/是否已连接/权限/建目录/写读删测试/写库结果；写入失败改为 toast 提示，不再静默。
- 面板显示「本页面所在文件夹」并与已连接的文件夹对比，选错文件夹时高亮警告。

## 2026-09-10 新增「本地版」（双击就用、数据在 db 文件夹、零授权点击）
- 纯 html 无法自读写磁盘（浏览器硬限制）→ 加一个本地小程序 `local/contacts_local.py`（纯标准库：静态页 + REST + SQLite）。
- 前端加 `serverSync.js`：http 打开且 /api/health 有响应 → 自动进入本地服务模式（隐藏授权引导/横幅，顶栏显示「🌐 本地版 · db/contacts.db」），数据 PUT 给本机服务写进 db/contacts.db；file:// 打开则维持原模式。
- 备份：本地版下「全部备份」直接 POST 给服务端写进 backup/（含每天自动一份 .db 快照）。
- 交付件：`local/`（启动.bat + 打包exe.bat + 通讯录.html + 使用说明.txt），压缩包在 drop 目录。
- 实测：http://127.0.0.1:19118 添加/刷新/备份全通，数据落 db/contacts.db（Python sqlite3 复核）、Excel 落 backup/。

## 2026-09-10 新增「省份」字段
- 数据结构加 province（联系人/表单/列表/SQLite/本地服务/导入导出全链路）。
- 添加页：省份下拉（34 省市自治区，可不填）；通讯录列表显示省份标签；桌面表格加「省份」列。
- 查询：编码/电话/姓名/省份 四者包含即命中；**每页 7 条，结果超过 7 条自动分页**（已实测 8 条命中 → 1/2 页）。
- Excel：导出加「省份」列，导入识别 省份/省/地区 表头；旧文件（无此列）导入省份为空。
- SQLite：浏览器端(sql.js)与本地服务端都加 province 列，**旧库缺列自动 ALTER 迁移**。
- 自测：test_logic.mjs 22 项通过（含省份导出还原）。

## 2026-09-10 省份字段定稿（仅手机 App 版本）
- 用户要求：省份就是一个**普通文本框**（跟姓名一样自己填），不是下拉选择；且**只改手机 App 版本**。
- 手机版：添加页新增「省份」文本框（可不填）、列表显示省份标签、查询支持省份包含匹配；每页 7 条超过即分页。
- 网页版/本地版界面已回退（不加省份 UI）；数据层仍保留 province 兼容（DB 列 / Excel 列），避免与 App 数据互导时丢字段。
